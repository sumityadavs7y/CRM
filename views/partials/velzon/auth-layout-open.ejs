<!doctype html>
<html lang="en" <%- htmlAttrs %>>
<head>
    <%- include('head', { title: pageTitle }) %>
</head>
<body>
    <div class="auth-page-wrapper pt-5">
        <% if (showAuthParticles) { %>
            <%- include('auth-background') %>
        <% } %>

        <div class="auth-page-content">
            <div class="container">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="text-center mt-sm-5 mb-4 text-white-50">
                            <div>
                                <a href="/" class="d-inline-block auth-logo">
                                    <span class="fs-24 fw-bold text-white">CRM</span>
                                </a>
                            </div>
                            <p class="mt-3 fs-15 fw-medium">CRM Platform</p>
                        </div>
                    </div>
                </div>

                <div class="row justify-content-center">
                    <div class="col-md-8 col-lg-6 col-xl-5">
                        <div class="card mt-4<%= authCardClass ? ' ' + authCardClass : '' %>">
                            <div class="card-body p-4">
