<!doctype html>
<html lang="en" <%- htmlAttrs %>>
<head>
    <%- include('head', { title: pageTitle }) %>
</head>
<body<% if (typeof user !== 'undefined' && user) { %> data-nav-brand-short="<%= user.navBrandShortLabel %>" data-nav-brand-full="<%= user.navBrandLabel %>"<% } %>>
    <div id="layout-wrapper">
        <%- include('app-topbar') %>
        <%- include('app-sidebar') %>

        <div class="main-content">
            <div class="page-content">
                <div class="container-fluid">
