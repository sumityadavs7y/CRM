<%- include('../../partials/velzon/app-layout-open', {
    pageTitle: pipeline.name + ' - CRM Setup',
    activeNav: 'crm-setup-pipelines'
}) %>

                    <div class="row">
                        <div class="col-12">
                            <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                                <div>
                                    <h4 class="mb-sm-0"><%= pipeline.name %></h4>
                                    <% if (pipeline.description) { %>
                                        <p class="text-muted mb-0 mt-1"><%= pipeline.description %></p>
                                    <% } %>
                                </div>
                                <div class="page-title-right">
                                    <ol class="breadcrumb m-0">
                                        <li class="breadcrumb-item"><a href="/dashboard">CRM</a></li>
                                        <li class="breadcrumb-item">CRM Setup</li>
                                        <li class="breadcrumb-item"><a href="/company/crm-setup/pipelines">Pipelines</a></li>
                                        <li class="breadcrumb-item active"><%= pipeline.name %></li>
                                    </ol>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-12 d-flex gap-2">
                            <a href="/company/crm-setup/pipelines" class="btn btn-light btn-sm">Back to Pipelines</a>
                            <% if (user.can('crm_setup', 'edit')) { %>
                                <a href="/company/crm-setup/pipelines/<%= pipeline.id %>/edit" class="btn btn-soft-secondary btn-sm">Edit Pipeline</a>
                            <% } %>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-12">
                            <%- include('_pipeline-config-tabs', {
                                pipeline: pipeline,
                                leadStages: leadStages,
                                dealStages: dealStages,
                                labels: labels,
                                canEdit: false,
                                activeTab: activeTab,
                                defaultLabelColor: defaultLabelColor
                            }) %>
                        </div>
                    </div>

<%- include('../../partials/velzon/app-layout-close') %>
